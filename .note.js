// Back end setup

// .env

// build from seperate folder

// "build:ui": "rm -rf build && cd ../../Fullstack/Phonebook && npm run build --prod && cp -r build ../../Fullstack/Phonebook-backend/",
// "deploy": "git push heroku master",
// "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy",
// "logs:prod": "heroku logs --tail",
